export interface Movie {
  title: string;
  posterUrl: string;
}

export interface FormDataInterface {
  firstName: string;
  lastName: string;
  age: string;
  hideEmail: boolean;
  email: string | null;
  comment: string;
}
